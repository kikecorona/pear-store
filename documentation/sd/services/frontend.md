# frontend

> *Auto-generated SD page.* Service `frontend` · revision `afc6e1738272` · content hash `afc6e1738272` · last updated 2026-06-27 05:54 UTC.

## Endpoints

- **`GET /account`** — handler `account`
  This endpoint handles GET and POST requests for account management. For POST requests, it supports login, register, and logout actions. If the action is successful, it redirects to the index page. For GET requests, it retrieves a user's library and pearcare plan if they are logged in.
- **`POST /account`** — handler `account`
  This endpoint handles account-related actions, including login, registration, and logout. It accepts POST requests with a specified action type (login, register, or logout) and performs the corresponding operation. If an error occurs during registration or login, it displays an error message.

## Downstream dependencies

_No downstream dependencies detected._


## Related products

_No B&P pages currently reference this service._
